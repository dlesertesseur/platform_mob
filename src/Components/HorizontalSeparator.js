import React from "react";
import { View } from "react-native";


const HorizontalSeparator = ({ height=15 }) => {
  return (
    <View style={{width: "100%", height: height}}>
    </View>
  );
};

export default HorizontalSeparator;

